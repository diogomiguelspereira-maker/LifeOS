package com.lifeos.drive

/**
 * Preenche com o teu domínio e uma chave de integração da LifeOS
 * (Definições → Automações → Gerar chave).
 */
object Config {
    const val BASE_URL = "https://lifeos-eosin-phi.vercel.app"
    const val API_KEY = "" // <-- cola aqui a chave (Bearer)
}
